#poll
  .question= @poll.question
  .description= @poll.description
  = partial 'polls/voting_options', :poll => @poll, :vote => Vote.new
  = partial 'polls/statistics'
  = partial 'adviser_voting', :poll => @poll